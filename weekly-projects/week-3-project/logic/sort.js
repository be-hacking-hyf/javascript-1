/* sort characters

  sort the characters in a string, regardless of what they are

*/

const sortTests = [
  { name: 'first', args: [] },
];
function sort(str) {
  // write me!
}
evaluate(sort, sortTests);
